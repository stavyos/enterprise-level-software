{
  "name": "prefect-orchestrator",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/prefect-orchestrator/src",
  "projectType": "application",
  "targets": {
    "install": {},
    "test": {},
    "lint": {},
    "format": {},
    "build": {},
    "run": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv run prefect server start --host 0.0.0.0",
        "cwd": "{projectRoot}"
      },
      "dependsOn": ["install"]
    },
    "worker": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv run prefect worker start --pool dev-k8s-pool --type docker",
        "cwd": "{projectRoot}"
      },
      "dependsOn": ["install"]
    },
    "worker:prod": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv run prefect worker start --pool prod-k8s-pool --type docker",
        "cwd": "{projectRoot}"
      },
      "dependsOn": ["install"]
    },
    "start": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "nx run prefect-orchestrator:run",
          "nx run prefect-orchestrator:worker"
        ],
        "parallel": true
      }
    },
    "start:prod": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "nx run prefect-orchestrator:run",
          "nx run prefect-orchestrator:worker:prod"
        ],
        "parallel": true
      }
    }
  }
}
